<!-- DELETE -->
<?php

// generate by github.com/mysql-workbench/mwb-laminas-mv-generator on 23/11/2025
// <root>/module/{{module|slugify}}/view/{{entity.name|slugify}}/{{ action }}.phtml:

$title = 'Delete City #' . {%for key, table in entity.dbPKColumns %}{{entity.name|variabilize}}->{{ key }}{%endfor%};

$url   = $this->url('{{module|slugify}}', ['controller'=>'{{entity.name|slugify}}', 'action' => '{{ action }}'{%for key, table in entity.dbPKColumns %}, 'id'=>{{entity.name|variabilize}}->{{ key }}{%endfor%}]);
?>
<div class="p-5 mb-4 bg-light rounded-3">
	<h1><?= $this->escapeHtml($title) ?></h1>

	<p>
	    Are you sure that you want to delete
	    "<?= $this->escapeHtml({{entity.name|variabilize}}->city) ?>"?
	</p>

	<form action="<?= $url ?>" method="post">
		<div class="form-group">
		    <input type="hidden" name="id" value="<?= (int) {%for key, table in entity.dbPKColumns %}{{entity.name|variabilize}}->{{ key }}{%endfor%}; ?>" />

		    <input type="submit" class="btn btn-danger" name="del" value="Yes" />
		    <input type="submit" class="btn btn-success" name="del" value="No" />
		</div>
	</form>
</div>

