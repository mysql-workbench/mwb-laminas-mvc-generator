{# <?php #}

	/* READ[{%for verb in methods %}{%if not loop.first%}|{%endif%}{{verb}}{%endfor%}] */
	public function {{action}}Action()
	{
		$rowset = $this->getTable()->select();
		$row = $rowset->current();

		return new ViewModel([
			'{{entity.name|variabilize(true, '')}}' => $rowset
		]);
	}

