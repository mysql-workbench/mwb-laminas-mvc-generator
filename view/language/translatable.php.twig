<?php

return [
{%for column in entity.dbTable.columns %}
	'placeholder-{{module|slugify}}-{{entity.name|slugify}}-{{column.name}}' => 'Value of {{column.name|verbalize(false, true)}}',
	'label-{{module|slugify}}-{{entity.name|slugify}}-{{column.name}}' => '{{column.name|verbalize(false, true)}}',
{%endfor%}
];

